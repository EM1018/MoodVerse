## Constructing dataset
To construct dataset and get train, dev, test splits, first navigate to root directory

```bash
python ./build_dataset.py
```
This will take a while

```bash
python ./split_dataset.py
```